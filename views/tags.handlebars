<section class="list-section" id="tags-section">
  <h2 class="section-header">tags</h2>
  <ul class="list" id="tags">
    {{#each tags}}
      <li class="item tag" data-tag-id="{{id}}">{{tag_name}}</li>
    {{/each}}
  </ul>
</section>