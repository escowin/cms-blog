{{!-- custom stylesheet --}}
<link rel="stylesheet" href="/css/homepage.css">

<h2 class="homepage-header">Journals</h2>
<ul class="journals">
  {{#each journals as |journal|}}
      {{!-- rendering the partial --}}
      {{> journal-info journal }}
  {{/each}}
</ul>